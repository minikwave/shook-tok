DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=
# 선택: 초대 URL 권한 비트(기본값 생략 시 채널 보기·메시지 전송·임베드·히스토리 읽기)
# DISCORD_INVITE_PERMISSIONS=84992
DISCORD_CLIENT_SECRET=
# 개발용: 이 길드를 지정하면 `pnpm commands:register` 시 해당 서버에만 명령이 등록되고 즉시 반영됩니다. 비우면 전역 등록.
# DISCORD_GUILD_ID=
# Docker Compose 기본값(docker-compose.yml과 동일):
DATABASE_URL=postgresql://shook:shook_dev@localhost:5432/shook_tok
REDIS_URL=
NODE_ENV=development
PORT=3000
